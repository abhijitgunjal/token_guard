# Core (always required)
tiktoken>=0.6.0

# Provider extras (install as needed)
# pip install -e ".[groq]"
transformers>=4.40.0       # exact Groq/HuggingFace token counts

# pip install -e ".[bedrock]"
boto3>=1.34.0              # AWS Bedrock CountTokens API

# pip install -e ".[fastapi]"
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0
